# Leave unset to use the Vite dev proxy (/api -> backend, see vite.config.ts).
# Set explicitly for production builds served separately from the API.
VITE_API_URL=/api

# Used only by vite.config.ts's dev-server proxy target.
VITE_API_PROXY_TARGET=http://localhost:4000
