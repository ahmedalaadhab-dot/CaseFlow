# Loaded automatically by `vite build` (mode=production) — no hosting
# platform config needed. Not a secret: this is the public API endpoint
# the browser calls directly, already visible in every network request.
VITE_API_URL=https://caseflow-govr.onrender.com/api
